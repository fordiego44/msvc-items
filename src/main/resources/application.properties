spring.application.name=msvc-items
server.port=8005

#esto lo puse para que no de error, en si deberia estar en un servidor de configuracion
configuracion.texto=Configurando ambiente por Defecto 
# spring.cloud.discovery.client.simple.instances.msvc-products[0].uri=http://localhost:8001
# spring.cloud.discovery.client.simple.instances.msvc-products[1].uri=http://localhost:9001

config.baseurl.endpoint.msvc-products=http://msvc-products

# eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka esta comentado porque reemplaza desplegaremos en produccion
eureka.client.service-url.defaultZone=${EUREKA_URL:http://eureka-server:8761/eureka}

# #Config  esta comentado porque desplegaremos en produccion
# spring.config.import=optional:configserver:
# spring.profiles.active=dev
# management.endpoints.web.exposure.include=*

# management.tracing.sampling.probability=1.0
# management.zipkin.tracing.endpoint=http://zipkin-server:9411/api/v2/spans